@* @model Gymify.Application.DTOs.Case.OpenCaseResultDto
@{
    ViewData["Title"] = "Case";
}

<h2 class="text-center">@Model.CaseName</h2>

<div id="case-container" class="case-container">
    <div id="items-strip" class="items-strip">
        @foreach (var item in Model.Items)
        {
                <div class="item">
                    <img src="@item.ImageUrl" alt="@item.Name" />
                    <p>@item.Name</p>
                </div>
        }
    </div>
</div>

<div class="text-center mt-3">
    <button id="openBtn" data-case-id="@Model.CaseId" class="btn btn-primary">Відкрити кейс</button>
</div>

<!-- Модальне вікно -->
<div id="resultModal" class="modal hidden">
  <div class="modal-content">
    <h2>Вітаємо!</h2>
    <img id="resultImg" src="" alt="" />
    <p id="resultName"></p>
    <button id="closeModal">Закрити</button>
  </div>
</div> *@