@using Newtonsoft.Json

@if (TempData["Success"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            alertify.set('notifier','position', 'bottom-right');
            var msg = @Json.Serialize(TempData["Success"]);
            alertify.success(msg, 0);
        });
    </script>
}

@if (TempData["Error"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            alertify.set('notifier','position', 'bottom-right');
            var msg = @Json.Serialize(TempData["Error"]);
            alertify.error(msg, 0); 
        });
    </script>
}

@if (TempData["Warning"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            alertify.set('notifier','position', 'bottom-right');
            var msg = @Json.Serialize(TempData["Warning"]);
            alertify.warning(msg, 0);
        });
    </script>
}

@if (TempData["Message"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            alertify.set('notifier','position', 'bottom-right');
            var msg = @Json.Serialize(TempData["Message"]);
            alertify.message(msg, 0);
        });
    </script>
}