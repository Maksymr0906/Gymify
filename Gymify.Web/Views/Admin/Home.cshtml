@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="ae-page-container">

    <h1 class="ae-page-title mb-4">👑 Admin Dashboard</h1>
    <p class="text-muted mb-4">
        Вітаю у панелі адміністратора!
        Тут ти керуєш наповненням, модеруванням та користувачами системи.
    </p>

    <div class="admin-grid">

        <a asp-action="ExercisePanel" class="admin-card">
            <div class="admin-card-icon">🏋</div>
            <div class="admin-card-title">Exercise Moderation</div>
            <div class="admin-card-desc">Перевіряй та підтверджуй нові вправи</div>
        </a>

        <a asp-action="CommentPanel" class="admin-card">
            <div class="admin-card-icon">💬</div>
            <div class="admin-card-title">Comment Moderation</div>
            <div class="admin-card-desc">Фільтруй коментарі та борись зі сміттям</div>
        </a>

        <a asp-action="UserPanel" class="admin-card">
            <div class="admin-card-icon">🧑‍🤝‍🧑</div>
            <div class="admin-card-title">Users</div>
            <div class="admin-card-desc">Бани, попередження, контроль ролей</div>
        </a>

        <a asp-action="WorkoutPanel" class="admin-card">
            <div class="admin-card-icon">🔥</div>
            <div class="admin-card-title">Workout Overview</div>
            <div class="admin-card-desc">Знайомся зі статистикою тренувань</div>
        </a>

    </div>

</div>

<style>
    .admin-grid {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        margin-top: 1rem;
    }

    .admin-card {
        background: #fff;
        border-radius: 12px;
        padding: 20px;
        text-decoration: none;
        color: #222;
        border: 1px solid #ddd;
        transition: .2s ease;
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

        .admin-card:hover {
            transform: translateY(-3px);
            border-color: #b38bff;
            box-shadow: 0 6px 14px rgba(0,0,0,0.08);
        }

    .admin-card-icon {
        font-size: 38px;
    }

    .admin-card-title {
        font-size: 1.2rem;
        font-weight: 600;
    }

    .admin-card-desc {
        font-size: .9rem;
        color: #777;
    }
</style>
